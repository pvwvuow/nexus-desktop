{
  "name": "nexus-desktop",
  "version": "1.0.6",
  "description": "NEXUS - Orbital Communication Platform",
  "main": "main.js",
  "author": {
    "name": "NEXUS Team",
    "email": "support@nexus.space"
  },
  "license": "MIT",
  "homepage": "https://github.com/pvwvuow/nexus-desktop#readme",
  "repository": {
    "type": "git",
    "url": "https://github.com/pvwvuow/nexus-desktop.git"
  },
  "bugs": {
    "url": "https://github.com/pvwvuow/nexus-desktop/issues"
  },
  "keywords": [
    "electron",
    "chat",
    "communication",
    "nexus",
    "desktop"
  ],
  "scripts": {
    "start": "electron .",
    "dev": "electron . --dev",
    "build": "electron-builder --win --publish never",
    "build:win": "electron-builder --win --publish never",
    "build:installer": "electron-builder --win --config.win.target=nsis --publish never",
    "build:portable": "electron-builder --win --config.win.target=portable --publish never",
    "build:mac": "electron-builder --mac --publish never",
    "build:linux": "electron-builder --linux --publish never",
    "clean": "node -e \"require('fs').rmSync('dist', {recursive:true, force:true})\""
  },
  "dependencies": {
    "electron-store": "^8.1.0",
    "electron-updater": "^6.1.7"
  },
  "devDependencies": {
    "electron": "^28.1.0",
    "electron-builder": "^24.9.1"
  },
  "build": {
    "appId": "com.nexus.desktop",
    "productName": "NEXUS",
    "directories": {
      "output": "dist",
      "buildResources": "build"
    },
    "files": [
      "main.js",
      "preload.js",
      "src/**/*",
      "build/icon.*"
    ],
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": ["x64"]
        }
      ],
      "icon": "build/icon.ico",
      "artifactName": "${productName}-Setup-${version}.${ext}",
      "publisherName": "NEXUS Team"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "allowElevation": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "NEXUS",
      "installerIcon": "build/icon.ico",
      "uninstallerIcon": "build/icon.ico",
      "installerSidebar": "build/installerSidebar.bmp",
      "deleteAppDataOnUninstall": true,
      "runAfterFinish": true,
      "artifactName": "${productName}-Setup-${version}.${ext}"
    },
    "portable": {
      "artifactName": "${productName}-Portable-${version}.${ext}"
    },
    "mac": {
      "target": ["dmg", "zip"],
      "icon": "build/icon.icns",
      "category": "public.app-category.social-networking",
      "artifactName": "${productName}-${version}-mac.${ext}"
    },
    "linux": {
      "target": ["AppImage", "deb"],
      "icon": "build/icon.png",
      "category": "Network",
      "artifactName": "${productName}-${version}-linux.${ext}"
    },
    "publish": {
      "provider": "github",
      "owner": "pvwvuow",
      "repo": "nexus-desktop"
    }
  }
}